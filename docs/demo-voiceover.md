[excited]
Brickwright brings LEGO building into your browser, with search across the full LEGO parts catalog!
That's over eighty thousand indexed part identities to explore. Find familiar bricks, discover
specialized pieces, and start designing your own models in an interactive three-dimensional workspace.

[enthusiastic]
For hands-on building, the editor includes nine hundred ready-to-place parts with real geometry.
Search by name or part number, choose a color, and drag a piece into your model. Connections use
actual studs and pin holes, so each placement follows the parts themselves.

[confident]
And Brickwright makes that same workspace available to AI agents through WebMCP! The app registers
its tools with the browser, giving an agent a structured description of each tool, its arguments,
and its results. Five site tools are available across every route.

[engaged]
Let's start on the landing page. The overview tool tells the agent where it is, which tools are
available, and what to do next. Watch this: one navigation call opens the editor and registers
the workspace tools over our live document!

[bright]
Here's our model: a brick wall ready for a roof. I'll search for a forty-five-degree slope and
select part thirty thirty-nine, a two-by-two sloped brick. Drag it onto the top edge, preview the
connection, and place it directly onto the wall.

[confident]
Now I can orbit the model and inspect that connection from another angle. Every edit runs through
the same connector logic, collision checks, and undo history. Those systems support both manual
building and agent actions, keeping the whole workflow consistent as we work.

[excited]
Here's Codex on my Mac, connected to this exact workspace. We're working on the same document,
with the wall I just edited. It discovers the WebMCP tools, reads the overview, and gets the
context it needs to help with the next step.

[animated]
I'll ask: add a row of roof slopes along the top of this wall, all facing outward.
Codex searches the catalog with the geometry filter enabled, selects ready-to-place parts, and
prepares the operations. You can follow those tool calls right beside the model.

[pleased]
In Propose mode, the agent runs preflight and presents its changes as a translucent preview,
right on the model. I can inspect the roof, review the placement, and accept the proposal.
Now those previewed pieces become part of our shared document!

[clear]
Three autonomy modes let me choose how we collaborate. Inspect lets the agent read and analyze.
Propose adds previews for my review. Build enables direct edits. Switching modes updates the
registered tools, so the agent's available actions follow the level I choose.

[energetic]
Let's switch to Build and ask Codex to extend the roof. It applies the changes directly through
the same editing system. Protected pieces stay in place, and edits remain in the undo history.
I can undo the addition, then redo it to compare.

[excited]
That's Brickwright: a searchable LEGO parts catalog, an interactive CAD editor, and an AI workflow
you can see directly in your build. Search for pieces, shape your model, and collaborate with
an agent in one workspace. Try it at brickwrite dot tech!
